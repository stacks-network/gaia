version: "3.8"

services:
  hub:
    env_file:
      - env-gcp.env

  admin:
    env_file:
      - env-gcp.env

  reader:
    env_file:
      - env-gcp.env

  nginx:
    env_file:
      - env-gcp.env
