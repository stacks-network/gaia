{
  "servername": {
    "doc": "The name of the server.",
    "format": "String",
    "default": "localhost",
    "env": "gaia_servername"
  },
  "port": {
    "doc": "The port to listen on.",
    "format": "port",
    "default": 3000,
    "env": "gaia_port"
  },
  "driver": {
    "doc": "The storage driver to use.",
    "format": ["aws", "azure", "disk", "google-cloud"],
    "default": "disk",
    "env": "gaia_driver"
  },
  "bucket": {
    "doc": "The bucket name for cloud based storage drivers.",
    "format": "String",
    "default": "",
    "env": "gaia_bucket"
  },
  "readURL": {
    "doc": "The readURL.",
    "format": "String",
    "default": "",
    "env": "gaia_readURL"
  },
  "whitelist": {
    "doc": "Whitelist of addresses that can use this gaia hub.",
    "format": "stringArrayOrFalse",
    "default": false,
    "env": "gaia_whitelist"
  },
  "diskSettings": {
    "storageRootDirectory": {
      "doc": "The root path for local disk based storage.",
      "format": "String",
      "default": "/tmp/gaia-disk",
      "env": "gaia_diskSettings_storageRootDirectory"
    }
  },
  "awsCredentials": {
    "accessKeyId": {
      "doc": "The AWS access key.",
      "format": "String",
      "default": "",
      "env": "gaia_awsCredentials_accessKeyId"
    },
    "secretAccessKey": {
      "doc": "The AWS secret access key.",
      "format": "String",
      "default": "",
      "env": "gaia_awsCredentials_secretAccessKey"
    }
  },
  "proofsConfig": {
    "proofsRequired": {
      "doc": "The number of social proofs required for write access.",
      "format": "Number",
      "default": 0,
      "env": "gaia_proofsConfig_proofsRequired"
    }
  },
  "azCredentials": {
    "accountName": {
      "doc": "The Azure account name.",
      "format": "String",
      "default": "",
      "env": "gaia_azCredentials_accountName"
    },
    "accountKey": {
      "doc": "The Azure account key.",
      "format": "String",
      "default": "",
      "env": "gaia_azCredentials_accountKey"
    }
  },
  "gcCredentials": {
    "projectId": {
      "doc": "The Google Cloud Project ID.",
      "format": "String",
      "default": "",
      "env": "gaia_gcCredentials_projectId"
    }
  },
  "argsTransport": {
    "colorize": {
      "doc": "Toggle log colorization.",
      "format": "Boolean",
      "default": true,
      "env": "gaia_argsTransport_colorize"
    },
    "handleExceptions": {
      "doc": "Toggle uncaught exception handling.",
      "format": "Boolean",
      "default": true,
      "env": "gaia_argsTransport_handleExceptions"
    },
    "json": {
      "doc": "Toggle json in logs.",
      "format": "Boolean",
      "default": true,
      "env": "gaia_argsTransport_json"
    },
    "level": {
      "doc": "The log level.",
      "format": ["error", "warn", "info", "verbose", "debug", "silly"],
      "default": "warn",
      "env": "gaia_argsTransport_level"
    },
    "stringify": {
      "doc": "Toggle stringify on objects in logs.",
      "format": "Boolean",
      "default": true,
      "env": "gaia_argsTransport_stringify"
    },
    "timestamp": {
      "doc": "Toggle timestamps in logs.",
      "format": "Boolean",
      "default": true,
      "env": "gaia_argsTransport_timestamp"
    }
  }
}